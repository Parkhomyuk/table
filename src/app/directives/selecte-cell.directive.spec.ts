import { SelecteCellDirective } from './selecte-cell.directive';

describe('SelecteCellDirective', () => {
  it('should create an instance', () => {
    const directive = new SelecteCellDirective();
    expect(directive).toBeTruthy();
  });
});
